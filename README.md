# testing-claude-code

A tiny static login demo built with plain HTML, CSS, and JavaScript.

## Run

Open `index.html` in a browser, or serve the directory:

```bash
python3 -m http.server 8080
```

Then visit http://localhost:8080.

## Demo credentials

| username | password    |
|----------|-------------|
| admin    | admin123    |
| krishna  | password1   |
| guest    | guest       |

Credentials are stored client-side in `users.js` for demo purposes only.
